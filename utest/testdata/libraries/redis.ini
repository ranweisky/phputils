[TestRedis]
ip=127.0.0.1
port=6379
;redis db index (default is 0)
index=15
;whether to keep the connection
keepalive=true
;value in ms (optional, default is 0 meaning unlimited)
timeout=2000

[InvalidIndexRedis]
ip=127.0.0.1
port=6379
;redis db index (default is 0): out of range
index=17
;whether to keep the connection
keepalive=true
;value in ms (optional, default is 0 meaning unlimited)
timeout=2500

[InvalidIpRedis]
ip=128.127.127.127
port=6379
;redis db index (default is 0): out of range
index=0
;whether to keep the connection
keepalive=true
;value in ms (optional, default is 0 meaning unlimited)
timeout=1000
